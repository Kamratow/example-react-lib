{
  "name": "example-lib-app",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "module": "dist/index.js",
  "files": [
    "dist/",
    "README.md"
  ],
  "dependencies": {
    "@babel/core": "^7.3.3",
    "cross-env": "^5.2.0",
    "rimraf": "^2.6.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rimraf dist && cross-env NODE_ENV=production npx babel src/lib --out-dir dist --copy-files",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "react": "^16.8.2",
    "react-dom": "^16.8.2",
    "react-scripts": "2.1.5"
  }
}
